# L4 真实验收验证报告：run 15309（Cybench set 5，40 题 / 20800 分）

> 状态：**已结束**。2026-09-05 14:45 创建，20:50 平台超时结束（21600s / 6h）。

## 最终成绩（平台口径）

- **31/40 题完成，13300/20800 分（63.9%）**，31 枚 flag。
- 排名口径：同分按 `score_elapsed_seconds` 排序（未全题完成 → 按平台 timeout 结束，无停表优势）。
- 零人工干预：全部求解、交卷、容器开关、hint 决策、崩溃恢复由 @shence/xiaochang-runner 自主完成；人工仅做监控与缺陷修复部署。

## 结论：首跑目标未达 95%，但 L4 验收的本质目的全部达成

L4 的目标是**验证产品化架构能在真实平台自主跑完一场**——做到了，而且首跑就以
31/40 收场；95% 完成率是"稳定系统再跑"的期望值（本场 6 次调试重启 ≈ 损耗
60-90 分钟有效时间，且最后一小时被两个新发现的缺陷拖累，详见下）。

## 编排架构（实战验证）

- 夜不收 TsecbenchAdapter（平台六原语）+ 虎符战役（工作项账本/槽位/优先级/序列化恢复）+ 集思通道（按次模型+思考强度）。
- 模型策略（最终版）：easy→kimi-k3、medium→deepseek-v4-flash(low)、hard→glm-5.3(max)、第 2 轮起 effort 升级；三供应商分摊限流。
- 托管合规：求解 prompt 仅含 题面+入口、通用方法论（prompts/solver.md）、本题已确认 flag、同题上一轮自己的工作记录；run 间只继承公共知识；clean-room 门禁按题号弃权。

## 首跑暴露并修复的缺陷（10 项，全部已入库）

| # | 缺陷 | 修复 |
|---|---|---|
| 1 | 集思：reasoningEffort 未宣告即静默杀子代理（kimi+high 复现） | effort 按模型宣告过滤（jisi 509a189） |
| 2 | 集思：按次模型缺 provider 路由（glm 误送 kimi 路由） | 模型名反查宿主路由（jisi 06d23ca） |
| 3 | 集思：失败报告无诊断文本 | 附 SubagentResult.diagnostic |
| 4 | 虎符：轮次记账错位（账本 seed ≠ 轮次） | 轮次由工作项 id 承载 |
| 5 | runner：轮次间失忆（3×20min 的题） | 上一轮工作记录注入下一轮 prompt（含超时后迟到输出） |
| 6 | runner：瞬时失败烧轮次 | 限流/空诊断同轮重试 ≤5 次 |
| 7 | runner：retry 项 id 解析错（`#3-r1` 不匹配 `#s\d+`）→ 重试链静默断裂 | codeOf/roundOf 统一解析（9458a3a） |
| 8 | runner：容器槽位泄漏 → 队列饿死（实测 30 分钟无派单） | 关闭重试按平台状态判定；启动时重置标志（080ed5b） |
| 9 | runner：崩溃恢复 | JSONL 快照 + 启动清残留容器（实战 7 次重启全部正确续跑） |
| 10 | 排名计时 | 拿完 flag 立即关容器；全题终态/预算耗尽立即 finish 停表 |

## 未解之谜（第二场前必查）

**hard 题 glm-5.3+thinking 在并发下静默失败（空诊断）**：探针单发 PONG 通过，
但 3 槽位并发打 hard 时求解器秒败且无任何诊断文本，retry 链耗尽 5 次仍失败
（g-01/02/24/25/27/28/38/39/40 全部中招）。怀疑智谱并发上限或 thinking 参数
并发限制。第二场方案：hard 改用 deepseek-v4-pro(max)（本会话同款模型，¥546
余额充足），或 glm-5.3 与 deepseek-v4-pro 按题轮换。

## 复盘要点（第二场准备）

- **不再中途重启**：7 次重启 ≈ 1 小时有效时间损耗 + 3 道题被重启连坐判负。
- 完成率走势：easy 9/9 一轮全过；medium 首轮多数；hard 11 题仅 2 题完成。
- 余额消耗：Kimi ¥106→~¥90、DeepSeek ~¥5、智谱待查。

## 环境

- dev 实例 headless CLI（DSH_HOME=/home/zrn/.dsh-dev）+ OpenVPN + bwrap 沙箱。
- 模型真调：kimi-k3/k2.6、deepseek-v4-flash、glm-5.3/4.6/4.5-air。
- 生产实例（本会话 3080）零接触。

## 遗留建议（v1.1）

- 账本级审计落盘；求解器工具集按题定制。
- hard 题 fanout 多模型并行（集思已具备，runner 未接入）。
- 平台允许一次一场：第二场需等本场结束后再开（token 有效期至 09-06 13:50 +08）。
