# L4 真实验收验证报告：run 15309（Cybench set 5，40 题 / 20800 分）

> 状态：**报告骨架，最终数字在 run 结束后回填**（预计 2026-09-05 20:45 平台截止）。

## 目标与结论（回填）

- 完成率：XX/40（XX%），得分 XXXX/20800。
- 零人工干预：除模型路由/缺陷修复导致的进程重启（快照续跑，无手工接管）外，全部求解、交卷、容器开关、hint 决策、停表均由 @shence/xiaochang-runner 自主完成。
- 排名口径：同分按 score_elapsed_seconds 排序；runner 全题终态/预算耗尽时立即 finish 停表。

## 编排架构（首轮实战验证）

- 夜不收 TsecbenchAdapter（平台六原语）+ 虎符战役（40 工作项账本/槽位/优先级/序列化恢复）+ 集思通道（按次模型+思考强度）。
- 模型策略（最终版）：easy→kimi-k3、medium→deepseek-v4-flash(low)、hard→glm-5.3(max)、第 2 轮起 effort 升级 max；三供应商负载分摊。
- 托管合规：求解 prompt 仅含 题面+入口、通用方法论（prompts/solver.md）、本题已确认 flag、同题上一轮自己的工作记录；run 间只继承公共知识；clean-room 门禁按题号弃权。

## 实战暴露并修复的缺陷（全部已入库）

1. 集思：reasoningEffort 未宣告即静默杀子代理（kimi-k2.6+high 复现）→ effort 按模型宣告过滤。
2. 集思：按次模型缺 provider 路由（glm 误送 kimi 路由）→ 模型名反查宿主路由。
3. 虎符：轮次记账错位（账本 seed ≠ 轮次）→ 轮次由工作项 id 承载。
4. runner：轮次间失忆 → 上一轮工作记录注入下一轮 prompt（含超时后迟到的输出）。
5. runner：瞬时失败烧轮次 → 限流/空诊断同轮重试 ≤5 次；失败题收尾复活重打。
6. runner：崩溃恢复 → JSONL 快照 + 启动清残留容器 + 进程死后再调即续跑（实战 6 次重启全部正确恢复）。
7. runner：排名计时 → 拿完 flag 立即关容器；全题终态/预算耗尽立即 finish 停表。
8. 平台侧：token 形态（JWT 会话 vs UUID benchmark token）、run 创建/状态/VPN 配置经前端 API 获取。

## 环境

- dev 实例 headless CLI（DSH_HOME=/home/zrn/.dsh-dev）+ OpenVPN（10.254.0.22）+ bwrap 沙箱。
- 模型：kimi-k3/k2.6、deepseek-v4-flash、glm-5.3/4.6/4.5-air 真调；余额监控 API（DeepSeek ¥546、Kimi ¥94）。
- 生产实例（本会话 3080）零接触。

## 遗留建议（v1.1）

- 稳定后再开一场干净 run 冲 95%+（首场 6 次调试重启 ≈ 1 小时有效时间损耗；平台限一场 active run，需本场结束后再开）。
- runner 账本级审计落盘（现仅进度账 + 审计轨迹）；求解器工具集按题定制。
- 硬题策略：glm-5.3 二轮未破时考虑 fanout 多模型并行（集思已具备，runner 未接入）。
