# L4 验证报告（第二场）：run 15451（Cybench set 5，40 题 / 20800 分）

> 2026-09-06 02:49 创建，08:25 finish 停表（elapsed 20133s ≈ 5.6h，未跑到 6h 超时）。

## 最终成绩（平台口径）

- **37/40 题完成，18700/20800 分（89.9%）**，37 枚 flag，final_score 89.9。
- 第一场（run 15309）对照：31/40、13300 分（63.9%）→ **+6 题 / +5400 分**。
- 3 道未完成全部是 hard（g-18、g-27、g-28，700-800 分/题），各耗尽 3 轮；
  easy 9/9 一轮全过，medium 20 题全清。
- 零人工干预：求解/交卷/容器开关/hint/停表全自主；本场**零中途重启**（第一场 7 次）。

## 两场之间的修复 → 直接换算成分数

| 修复（仓/提交） | 效果 |
|---|---|
| 智谱余额恢复（充值 ¥200）+ hard 换 deepseek-v4-pro（18a1a49） | hard 11 题从 2/11 → 8/11 |
| 容器槽位泄漏（080ed5b） | 尾段不再饿死（第一场因此白等 30+ 分钟） |
| retry id 解析（9458a3a） | 瞬时失败重试链真正生效 |
| 轮次续跑/迟到输出继承（21fd763） | 多轮题不再重复侦察 |
| 当前代模型线（dd84d2f） | easy/medium 一轮通过率大幅提升 |
| **工作目录移出 checkout（ext4）** | Defender 不再报毒/隔离；checkout 全程 0 污染 |

## 本场新发现并已修复的缺陷

1. **finishRun 传错变量**（8c0a3d9）：停表调用用了 `config.baseURL`（undefined）→
   `Failed to parse URL from undefined/...`，排名计时多走了 5 分钟才被手动停表。
   → 改为传入解析后的局部 `baseURL`。
2. **求解器工作区污染 + Defender 报毒**（633cbfe 文档约定）：求解器 bash 默认 cwd =
   进程 cwd，第一场把真实攻击样本（mail.eml、malware.exe、pcap）写进了 checkout
   （/mnt/d，Windows 可见）触发报毒。→ 现在从 `/home/zrn/xiaochang-work`（ext4）
   启动，Defender 不扫、沙箱 workspace-write 圈内。
3. 金柝 `guard-runner.sh` 上线（2d21619）：跑分进程守护（死→重拉、假死→杀重拉、
   内存只告警），混沌测试 2/2 通过。

## 归档

- 本地快照/审计轨迹（含求解过程与 flag 值，**不入公开仓**）：`/home/zrn/.dsh-dev/storages/xiaochang-run*.jsonl`（run 15451 时段）。
- 余额消耗：DeepSeek ¥546.65→¥455.75（约 ¥91）；Kimi 平台余额现 ¥232.22。

## 结论

神策四仓 + runner 的链路在真实平台连续两场 6 小时级战役中被验证：
**修复-复跑-对比的闭环成立**（两场 +5400 分），且每场都在产出新的真实缺陷并被
及时修复——这正是"验证项目群有效"的核心目的。剩余 3 道 hard 题属求解能力边界
（多轮仍未破），不影响链路有效性结论。
