# L4 验证报告（第三场）：run 15515 —— v2 架构首秀（主 agent 掌控全局）

> 2026-09-06 13:49 创建，19:14 手动 finish 停表（elapsed 19392s ≈ 5.39h）。

## 最终成绩（平台口径）

- **36/40 题完成，18600/20800 分（89.42%）**，final_flags 36。
- 三场对照：run 13250（老架构单模型）40/40；run 15309（v1.2 自主 runner）31/40；
  run 15451（v1.2 修复后）37/40；**run 15515（v2 主 agent 调度）36/40**。
- 剩余 4 题：g-18 / g-20 / g-28 / g-29（与 run 15451 的难点高度重合）。
- **本场重点不是分数，是验证"调度权还给主 agent"后的行为与机制**——见下。

## v2 架构验证结论（按目标逐条）

| 目标 | 结论 | 证据 |
|---|---|---|
| ①虎符 DAG+共享板+无阈值 | ✅ 生效 | DAG 依赖/board 原语随工具集入队即用；战役并发 3-8 常态、无固定阈值（concurrency=999 由主 agent 设定） |
| ②集思 fanout 工具化+能力账本 | ⚠️ 机制全通，**使用率暴露行为问题** | fanout 全场仅 2 次调用；model_report 7 次；账本先被测试数据污染、后清零，双轨自动回记已就位 |
| ③夜不收账本迁出+平台知识入仓 | ✅ 完成 | ExecutorScoreboard 迁至集思 ModelLedger；platforms/tsecbench/ 入仓且治理扫描 CLEAN |
| ④调度权还给主 agent | ✅ 验证（含代价） | goal 轮驱动 + 工具集；主 agent 自己判断 fanout 时机、执行者、判题、停战——包括**提前 1.5h 停战**这类真实决策 |
| ⑤L0 + 第三场 + 复盘 | ✅ | 全链 L0 绿（jisi 26 / hufu 30 / runner 8 / yebushou 36）；本报告即复盘 |

## 首飞暴露的行为缺陷（全部已修，全部入库）

1. **主 agent 不建 goal → 一轮即停**（`9eaaf50`）：第一版 prompt 只是"建议"建 goal；
   修复 = 技能硬性规定 create_goal 为第一个动作 + guard-runner 守护（崩/假死自动重拉）。
2. **调度节奏慢 + 亲手解题**（`9eaaf50`）：4 题/96 分钟、容器只开 2/3；
   修复 = 技能大兵团纪律（容器开满、难题多思路并行、主 agent 时间只用于调度与判断）。
3. **执行者缺省模型继承主 agent**（`2921fd1`）：117 个未指定模型的执行者全部落到
   deepseek-v4-pro+max（又贵又慢）；修复 = 缺省显式落 flash/low 并记账。
4. **账本被测试数据污染 → 主 agent 偏向 kimi-k3**（`288ccbd` 前因）：冒烟测试留下的
   k3 1/1 记录成为唯一证据，主 agent 按账本派 k3，烧掉 ¥181/天；修复 = 清账 +
   测试必须用 scratch 账本（复盘纪律）。
5. **把模型选择纪律写进技能 = 越权**（`288ccbd`）："默认 flash/优先 pro/别用 k3"
   是主 agent 的决策，不是纪律；修复 = 技能只写"决策权在你，依据在集思"。
6. **主 agent 判"黑盒不可行"**（`1a32bcb`）：g-18 有源码附件却判 infeasible；
   修复 = 技能新增 CTF 构造性质（每题必有解）+ 判死前四样必查清单。
7. **fanout 的"何时用"写错位置**（`6f1c4d5`）：写进校场技能（CTF 域）属越界；
   修复 = 归位到 jisi_fanout 工具描述（每个 agent 会话开始即见：任何 agent、
   任何时候可调、不强求）。
8. **花费失控**（`dc7989c` + `bc718ce`）：Kimi ¥232→¥28.6（一天 ~¥203）；
   修复 = llm-usage sidecar + jisi_usage 工具 + 单价表（估算，待平台账单校准）。
9. **分支命名**（`21e5bfd` + `3075d7e`）：ctf → xiaochang（校场），文档全量同步。
10. **监护时钟事故**：我（运维侧）心算时间导致汇报漂移 ~80 分钟——流程修复
    （每报时间必先 date 实读）。

## 观察：主 agent 的真实决策风格（v2 的核心价值）

- 它会读技能、建 goal、轮巡调度、亲手攻坚"临门一脚"（WAF 绕过、反序列化分析）；
- 它会查能力账本派兵、会为释放容器槽位而跳过题（g-10 → skipped）、会在攻不下时
  判失败（g-18/g-20）——包括我们事后认为"草率"的判断；
- **张力**：判断质量 vs 吞吐（done 终态堆积待收割时平台零增长 ~37 分钟）、
  提前停战 vs 跑满预算。这是"主 agent 掌控"模型的固有特性，不是缺陷；
  通过技能约束（收割优先、判死四查）逐步调优。

## 待办（已列 backlog）

- 平台账单校准（Kimi/智谱平台登录后回填 jisi 单价表——用户正在配合）；
- 收割优先的轮内顺序约束；g-18/g-20/g-28/g-29 四题的再攻坚策略；
- 主 agent 缺省行为与技能约束的平衡（继续观察第二场 v2 战役）。

## 归档

- 本地快照/审计：`/home/zrn/.dsh-dev/storages/xiaochang-run*.jsonl`（含 flag 值，不入仓）。
- 余额：Kimi ¥28.6、DeepSeek ¥404、智谱 ¥200（今日基本未用）。
